version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - echo $BUCKET
artifacts:
  files:
    - 'build/**/*'
  name: '/'
